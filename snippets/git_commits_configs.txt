   
# Show the files changed by a commit.
   git diff-tree --no-commit-id --name-status -r <HEAD/commit-sha>
  
# Configure user name and email
   git config user.name "Name"
   git config user.email "email@domain"